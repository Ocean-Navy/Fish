server {
  listen 80 default_server;
  listen [::]:80 default_server;
  server_name op.fish www.op.fish _;
  client_max_body_size 2m;
  # Set this to FISH_PROXY_HEADER_SECRET only when FISH_TRUST_PROXY_HEADERS=true.
  # Keeping it blank strips any user-supplied X-Fish-Proxy-Secret header.
  set $fish_proxy_secret "";

  location ^~ /.well-known/acme-challenge/ {
    auth_basic off;
    root /var/www/letsencrypt;
    default_type "text/plain";
  }

  location = /api/health {
    auth_basic off;
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Fish-Proxy-Secret $fish_proxy_secret;
  }

  location / {
    return 301 https://op.fish$request_uri;
  }
}

server {
  listen 443 ssl http2 default_server;
  listen [::]:443 ssl http2 default_server;
  server_name op.fish www.op.fish _;
  client_max_body_size 2m;
  # Set this to FISH_PROXY_HEADER_SECRET only when FISH_TRUST_PROXY_HEADERS=true.
  # Keeping it blank strips any user-supplied X-Fish-Proxy-Secret header.
  set $fish_proxy_secret "";

  ssl_certificate /etc/letsencrypt/live/op.fish/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/op.fish/privkey.pem;

  location = /api/health {
    auth_basic off;
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Fish-Proxy-Secret $fish_proxy_secret;
  }

  location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Fish-Proxy-Secret $fish_proxy_secret;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }
}
