# Copy to a private file such as .env.ocean-proof.local.
# Do not commit the private file.

# Adapter HTTP service.
OCEAN_WORKLOAD_ADAPTER_HOST=127.0.0.1
OCEAN_WORKLOAD_ADAPTER_PORT=8787
# Modes:
# - dry_run: no successful proof
# - live: official Ocean CLI startFreeCompute/startCompute path with published DIDs
# - local_ocean_node: signed direct /freeCompute job on our own Ocean Node
OCEAN_WORKLOAD_ADAPTER_MODE=dry_run
OCEAN_WORKLOAD_ADAPTER_API_KEY=change-me
OCEAN_WORKLOAD_ADAPTER_DATA_DIR=data/ocean-workload-adapter

# Ocean CLI checkout. The official Ocean CLI flow uses the Ocean repo with
# npm run cli -- <command> ...
OCEAN_CLI_DIR=.deps/ocean-cli
# Optional direct binary override. Leave empty for the repo flow above.
FISH_OCEAN_CLI_BIN=

# Dedicated proof wallet and chain RPC. The Ocean CLI address bundle must support
# the selected chain, or ADDRESS_FILE must point at a custom Ocean contracts
# address file for that chain.
PRIVATE_KEY=
MNEMONIC=
RPC=
ADDRESS_FILE=

# Selected Ocean / Oncompute node and workload inputs.
NODE_URL=
FISH_OCEAN_DATASET_DIDS=[]
FISH_OCEAN_ALGO_DID=
FISH_OCEAN_COMPUTE_ENV_ID=
FISH_ALGORITHM_FILE_URL=
FISH_ALGORITHM_CHAIN_ID=8453

# Paid jobs only. Leave empty for startFreeCompute.
FISH_OCEAN_PAYMENT_TOKEN=
FISH_OCEAN_RESOURCES=
FISH_OCEAN_ADAPTER_PROVIDER_COST_USD=

# Optional durable output JSON if the selected algorithm supports it.
FISH_OCEAN_OUTPUT=

# local_ocean_node mode. Requires the Ocean Node compute environment to allow
# free image builds. This runs a tiny raw-code Python proof inside Ocean C2D.
FISH_OCEAN_LOCAL_BASE_IMAGE=python:3.11-slim
FISH_OCEAN_LOCAL_RESOURCES=
FISH_OCEAN_LOCAL_RESULT_TIMEOUT_MS=900000

# CLI command names are configurable because Ocean CLI docs and releases can drift.
AVOID_LOOP_RUN=true
FISH_OCEAN_START_FREE_COMMAND=startFreeCompute
FISH_OCEAN_START_PAID_COMMAND=startCompute
FISH_OCEAN_DOWNLOAD_COMMAND=downloadJobResults
FISH_OCEAN_DOWNLOAD_ARGS_TEMPLATE={jobId} {index} {outputDir}
FISH_OCEAN_RESULT_INDEX=0
FISH_OCEAN_RESULT_TIMEOUT_MS=900000
FISH_OCEAN_POLL_INTERVAL_MS=15000
